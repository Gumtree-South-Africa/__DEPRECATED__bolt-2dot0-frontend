{{this.content.searchFormModel.searchForm.seoAttributes}}
{{#if this.content.searchFormModel.searchForm.seoAttributes}}
    {{i18n "header.breadcrumb-search" ""}}
{{/if}}
{{#if this.header.breadcrumbs.currentCat}}
    {{this.header.breadcrumbs.currentCat.name}}
    {{#with this.header.breadcrumbs.currentLoc}}
        {{i18n "header.breadcrumb-search" this.name}}
    {{/with}}
{{else}}
    {{#with this.header.breadcrumbs.currentLoc.name}}
        {{this}}
    {{/with}}
    {{#with this.header.breadcrumbs.currentCat.name}}
        {{this}}
    {{/with}}
{{/if}}

<div {{#if buttonId}}id="{{buttonId}}"{{/if}} class="{{extraClasses}} btn-wrapper">
  <a class="btn" href="{{buttonLink}}">
    <span class="sudolink">
        {{#if i18nPath}}
            {{i18n i18nPath}}
        {{else}}
            {{i18n "header.postAd"}}
        {{/if}}
    </span>
    <span class="btn-icon {{#if hideIcon}}hidden{{else}}{{iconName}}{{/if}}"></span>
  </a>
</div>
